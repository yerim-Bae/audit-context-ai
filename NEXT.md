# 지금 할 일 — 클로드 코드 작업 순서

작성 2026-08-10 · `HANDOFF.md`를 먼저 읽고 이 문서로 온다.
각 단계에 **붙여넣을 프롬프트**와 **끝났다고 볼 조건**이 있다. 조건을 못 채우면 다음 단계로 가지 마라.

---

## 먼저 — DART는 이미 붙어 있다

새로 연결할 게 없다. 저장소에 이미 있는 것:

| 있는 것 | 내용 |
|---|---|
| `.env` / `.env.example` | `DART_API_KEY` |
| `npm run dart:check` | 키 동작 확인 |
| `npm run dart:find -- "회사명"` | 회사명 → 고유번호 (corpCode.xml을 `.cache/`에 30일 캐시) |
| `npm run dart:fetch -- <고유번호>` | 사업보고서 원문 XML 수집 + 섹션 분해 + sha256 + `dart-manifest.json` |
| `src/ingest/candidates.ts` | 원문에서 근거 후보 추출 |
| `dist/candidates.html` | 근거 후보 사람 검토 화면 |
| `src/render/companyPage.ts` | 회사 특정 화면 (하나투어 사례) |
| ADR 0007 | DART 수집 허용 결정 |

**따라서 진짜 남은 일은 "DART 연결"이 아니라 "DART로 받은 회사 원문을 카드덱에 이어 붙이는 것"이다.**

그리고 이게 결정적으로 중요하다:

> 팩 카드(유튜브 출처)는 **FACT가 될 수 없다**. C등급 단일 화자다.
> DART 사업보고서는 **A등급이고 `can_support_scope`에 COMPANY가 들어 있다**. 즉 **회사 카드는 FACT가 될 수 있다.**
>
> 이 비대칭이 제품의 핵심이다. 화면에서 두 종류가 눈에 띄게 달라 보여야 한다.

---

## 0단계 — 자리 잡기 (30분)

### 0-1. OneDrive 밖으로 옮기고 git 시작

```bash
# PowerShell
robocopy "$env:USERPROFILE\OneDrive\Desktop\감사맥락AI" "$env:USERPROFILE\dev\audit-context-ai" /E /XD node_modules .cache _to_delete
cd "$env:USERPROFILE\dev\audit-context-ai"
```

```bash
git init
git add -A && git commit -m "chore: 산업팩 도입 전 스냅샷"
git switch -c feat/industry-packs
```

`.gitignore`에 이게 들어 있는지 확인하라. 없으면 추가하고 커밋에서 빼라.
```
node_modules/
.cache/
.env
dist/
_to_delete/
seed/*/sources/*.xml
```
`seed/hanatour/sources/`에 5MB짜리 XML이 있다. 원문 자체를 git에 넣을지는 결정이 필요하다 — 넣으면 근거 재현성이 확보되고, 빼면 저장소가 가볍다. **매니페스트(sha256 포함)는 반드시 커밋한다.**

### 0-2. 붙여넣을 프롬프트

```
HANDOFF.md와 CLAUDE.md.patch.md를 읽고, CLAUDE.md에 패치를 반영해줘.
전체 교체가 아니라 절 단위 수정이다. 반영한 절과 반영하지 않은 절을 이유와 함께 보고해줘.
package.json에 "build:deck": "node scripts/build-deck.ts" 를 추가하고,
format 글롭에 packs/**/*.json 을 넣기 전에 npm run format 을 먼저 돌려줘.
```

### ✅ 끝났다고 볼 조건
```bash
npm install
npm run build:deck      # dist/deck-shipbuilding.html 생성
npm test                # 기존 검사 + 팩 무결성 18항목 전부 통과
npm run typecheck
npm run format:check
```
그리고 `dist/deck-shipbuilding.html`을 **브라우저로 직접 열어** 시작 화면 → 카드 이동 → 검색이 동작하는지 눈으로 본다. 다크모드도 확인.

---

## 1단계 — 팩 정합화 (1시간)

### 1-1. 카드 규격 위반 4건 수정

```
packs/shipbuilding/cards.json 이 자기 규격을 어기는 곳이 있다.
tests/pack-integrity.test.ts 를 먼저 조여서 아래를 실패로 만들고, 그다음 카드를 고쳐줘.

- C15: 표가 8행이다. 상한 6행
- C06: 본문 796자. 하한 800자
- C26: 표가 2개다. 상한 1개
- C01, C04: 다음 질문이 5개다. 규격은 3~4개

마지막 항목은 규격을 3~5로 완화하는 선택지도 있다. 어느 쪽이 나은지 판단해서
docs/onboarding-deck-design.md 의 카드 규격 절과 prompts/pack/03-cards.md 를 일치시켜줘.
내용을 줄일 때 수치나 고유명사를 버리지 마라.
```

### 1-2. field-matrix 만들기 — 파이프라인 첫 시험

```
prompts/pack/04-field-matrix.md 를 읽고, 조선업의 field-matrix.json 을 만들어줘.
근거는 packs/shipbuilding/knowledge/ 의 8개 문서다.

만든 뒤에 반드시 역방향 대조를 해줘:
- 매트릭스에서 '필수'인데 카드 26장에 없는 개념
- 카드에는 있는데 매트릭스에서 '불필요'로 판정된 개념
이 두 목록을 보고해줘. 카드가 매트릭스보다 먼저 만들어져서 순서가 뒤집혔기 때문에,
이 대조가 파이프라인이 실제로 작동하는지 보는 시험이다.
```

### ✅ 끝났다고 볼 조건
`npm test` 통과 + `packs/shipbuilding/field-matrix.json` 생성 + 역방향 대조 결과가 보고됨. 불일치가 나오는 건 정상이다. **불일치를 숨기지 말고 기록**하라.

---

## 2단계 — DART로 조선사 실제로 받아보기 (30분)

지금은 하나투어(여행업) 데이터만 있다. 조선업 회사를 실제로 받아 파이프라인이 도는지 본다.

```bash
npm run dart:check
npm run dart:find -- "HD현대미포"        # 조선사
npm run dart:find -- "대양전기공업"       # 조선기자재사
npm run dart:fetch -- <고유번호>
```

두 곳을 받는 이유: 카드덱의 밸류체인 위치 선택지가 **조선사 / 조선기자재사 / 해운사**인데, 앞의 둘을 실제 데이터로 검증해야 위치별로 화면이 달라지는지 알 수 있다.

### 붙여넣을 프롬프트

```
방금 dart:fetch 로 받은 회사 원문의 섹션 목록을 보여줘.
사업보고서 중에서 아래 항목이 어느 섹션에 있는지 찾아줘.

- 사업의 개요 / 주요 제품 및 서비스
- 원재료 및 생산설비
- 매출 및 수주상황
- 수익인식 회계정책 (주석)
- 핵심감사사항 (KAM)
- 특수관계자 거래 주석
- 우발부채·약정사항 주석

찾은 것과 못 찾은 것을 구분해서 보고해줘. 못 찾은 건 지어내지 마라.
```

### ✅ 끝났다고 볼 조건
`seed/<회사>/sources/dart-manifest.json`이 생기고 sha256이 기록된다. 위 7개 항목 중 무엇이 있고 무엇이 없는지 목록이 나온다.

> 이 단계에서 **실제 감사 대상 회사를 넣지 마라.** 상장사 공개 공시만 쓴다. README 경고 참조.

---

## 3단계 — 회사 오버레이 (반나절, 이번 목표의 본체)

목표는 이것 하나다.

> **시작 화면에서 회사를 고르면, 산업 카드 26장 뒤에 "이 회사는 산업 표준과 무엇이 다른가" 카드가 붙는다.**

### 3-1. 데이터 구조 먼저

```
회사 오버레이를 설계해줘. 코드를 쓰기 전에 구조부터 제안해줘.

요구사항:
- packs/<industry>/ 는 산업 일반이다. 회사 데이터는 여기 넣지 않는다.
- 회사 데이터는 기존 seed/<company>/ 규약을 따른다 (case.json, claims.json, sources/).
- 카드덱은 (팩, 회사) 조합으로 렌더된다. 회사가 없으면 지금처럼 산업 카드만 나온다.
- 회사 카드의 모든 주장은 DART 원문 근거(sha256 + 섹션 locator)를 갖는다.
  근거 있는 것만 FACT, 나머지는 UNVERIFIED.
- 팩 카드는 절대 FACT가 될 수 없다 (ADR 0010).

docs/decisions/0009, 0010 과 src/domain/pack.ts, src/domain/model.ts 를 읽고,
기존 Claim 모델을 재사용할지 새 타입을 만들지 판단해서 제안해줘. 새 타입을 만들자면 이유를 대라.
```

**여기서 새 개념을 만들지 않는 게 중요하다.** 기존 `Claim` + `EvidenceSpan` + `Scope` + `AssertionStatus`로 표현되면 그렇게 하라.

### 3-2. 첫 회사 카드 3장

전부 만들지 말고 **3장만** 만든다. 파이프라인이 도는지 보는 게 목적이다.

```
회사 카드 3장을 만들어줘. 근거는 dart:fetch 로 받은 사업보고서 원문뿐이다.

CO-01 이 회사는 무엇을 파는가
  - 사업부문, 주요 제품, 매출 구성
  - 근거: '사업의 개요', '주요 제품 및 서비스' 섹션

CO-02 산업 표준과 다른 점
  - 표: 항목 / 산업 표준(팩 카드 인용) / 이 회사 / 근거 / 상태
  - 근거가 없는 행은 반드시 UNVERIFIED 로 남긴다. 추측으로 채우지 마라.

CO-03 이 회사에 물어볼 것
  - CO-02 의 UNVERIFIED 행에서 자동 생성
  - 각 질문은 어느 주장에서 나왔는지 역추적된다

규칙:
- 원문에 없는 것을 쓰지 마라. 없으면 UNVERIFIED 다.
- 모든 FACT 는 섹션 locator 와 짧은 인용을 갖는다.
- 산업 근거로 회사 주장을 FACT 로 만들지 마라 (도메인 규칙 4).
```

### 3-3. 화면에서 구분

```
카드덱에서 산업 카드와 회사 카드가 눈에 띄게 다르게 보이게 해줘.
색만으로 구분하지 마라 (CLAUDE.md UI 규칙). 기호와 글자를 함께 쓴다.
회사 카드의 FACT 는 근거를 누르면 원문 섹션이 열려야 한다 —
기존 companyPage.ts 가 근거를 어떻게 여는지 보고 같은 방식을 써라.
```

### ✅ 끝났다고 볼 조건

한 사람이 브라우저에서 이걸 끝까지 할 수 있다:

1. 회사를 고르고 밸류체인 위치·담당 필드를 고른다
2. 산업 카드를 3~4장 읽는다
3. "이 회사는 산업 표준과 무엇이 다른가"로 넘어간다
4. 차이표의 한 행에서 근거를 눌러 **DART 원문 해당 섹션**을 본다
5. 근거가 없는 행이 **UNVERIFIED로 보이고**, 그것이 "물어볼 것" 카드에 질문으로 올라와 있다
6. 그 질문에서 다시 원래 주장으로 역추적된다

여기까지 되면 제품의 뼈대가 선 것이다. 나머지는 카드 수를 늘리는 일이다.

---

## 4단계 — 산업 확장 (이후 반복)

원자료 9종이 있다: 반도체, 이차전지, 원전, 헬스케어, 우주항공, AI, 피지컬AI, 자율주행, 풍력.

산업 하나당:
```
prompts/pack/01-source-note.md  → 편당 1회 (병렬 가능)
prompts/pack/04-field-matrix.md → 1회   ← 카드보다 먼저
prompts/pack/02-merge-pack.md   → 1회 (섹션별 분할)
prompts/pack/03-cards.md        → 1회
```

**첫 확장은 원전을 권한다.** 수주·마일스톤·진행률·계약변경 골격이 조선업과 거의 같아 제작 비용이 가장 낮고, 프롬프트가 산업을 갈아끼워도 작동하는지 가장 빨리 알 수 있다. 저변으로는 반도체·이차전지가 위지만 골격이 새로 필요하다. 근거는 `packs/README.md`.

산업을 늘리기 전에 **3단계를 먼저 끝내라.** 산업 10개짜리 학습 자료보다, 산업 1개 + 회사 연결이 되는 물건이 훨씬 쓸모 있다.

---

## 이번 주 안에 여기까지

```
[0] git + 배치 + 빌드 통과 + 브라우저 확인          — 30분
[1] 카드 규격 4건 수정 + field-matrix + 역방향 대조  — 1시간
[2] 조선사·기자재사 각 1곳 DART 수집                — 30분
[3] 회사 카드 3장 + 근거 열림 + 질문 역추적          — 반나절
```

3단계까지 끝나면 남은 건 반복 작업이다.

---

## 하지 말 것

- 회사 데이터를 `packs/` 에 넣지 마라. 팩은 산업 일반이다.
- 유튜브 근거로 회사 주장을 FACT로 만들지 마라.
- 실제 감사 대상 회사와 실제 감사 자료를 저장소에 넣지 마라.
- 3단계 전에 산업을 늘리지 마라.
- 돌리지 않은 검사를 통과했다고 보고하지 마라.
